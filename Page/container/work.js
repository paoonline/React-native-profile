import React from "react";
import { StyleSheet, Text } from "react-native";

const styles = StyleSheet.create({
    header: {
      fontSize: 20
    },
  });

const Work = () => <Text style={styles.header}>ประสบการณ์ทำงาน</Text>;

export default Work